addEventListener(`DOMContentLoaded`,()=>{
    // 50# DEFINIR VARIABLES NUEMRIAS
    // # DATOS DE ENTRADA
    // ## OPERACIONES BASICAS: + /*
    let c=Number(prompt(`ingrese los grados celcius:`))
    // #PROCESOS U OPERACIONES
    let f= 9/5*c+32
    // #salida o entrada de resultado
    console.log(`en grados Fahrenheit:°${f}`);
})